# Frozen source excerpts

This directory contains only the minimal public comparison data needed to reproduce R003 without network access.

- `ct_competition_2023_industrial8_t3.csv` — the six `INDUSTRIAL_8`, strength-3 rows extracted from the pinned CT-Competition 2023 `output_best.csv`.

The complete provenance, commit IDs, Git blob IDs, and comparison boundary are documented in [`../SOURCE_AUDIT.md`](../SOURCE_AUDIT.md).

Do not replace these files from mutable branch URLs without updating the source audit and deterministic hashes.
